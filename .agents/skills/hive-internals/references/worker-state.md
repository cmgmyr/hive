Worker state (working/idle/waiting) comes from Claude Code hooks writing directly to the database, keyed by `HIVE_AGENT_ID`. hive depends here on a payload contract it does not own, cannot see change, and pins no version of.

## A state that corrects itself still has to be readable afterwards

`agents.agent_state` is one row overwritten in place, so a wrong value replaced a second later leaves nothing behind. Issue #24 was closed on that basis and reopened the same day: a false `idle` was written at 13:27:39 and overwritten with `working` at 13:27:41, the lead sampled at 13:28, saw `working`, and recorded a PASS on a lane that had already failed. No polling frequency anyone would really run catches two seconds.

So `agent_state_log` is append-only and the hook writes one row per invocation. Nothing updates a row and nothing deletes one except retention. Three things about it are load-bearing:

- **The `event` column.** Both #24 lanes reasoned from "an idle was written" to "`stateFor("stop")` wrote it" without checking, and the truth was that the notify branch wrote it. One column answers that at a glance.
- **The payload is stored raw and unredacted.** The bug turned on `notification_type`, a field nothing in hive read. A projection can only preserve fields someone already knew mattered, which is the same incomplete-corpus failure wearing a disguise.
- **The log write sits AFTER the state write, in its own try/catch, never sharing a transaction.** The state write is what wakes and `agent_status` depend on, so a log that cannot be written must cost the diagnosis, never the behaviour.

Assert over the SEQUENCE in this table, never over a sample of `agent_state`, or the test has the blind spot the table exists to remove. A sample is not evidence about a state machine; an event record is.

Retention is `pruneStateLog()` in `tick()` rather than in the janitor, because the janitor returns early when the tmux probe fails and a machine can sit that way for days.

## Enumerate every path that can write the value, before picking one

Two full lanes of #24 reasoned from an observed value to a presumed writer and neither checked. Three lines of `stateFor` return `idle` and only one was ever under investigation. The first fix was correct and fixed a door the bug was not coming through; it shipped with 50 green tests and the bug reproduced the same morning.

Any time you have "the system recorded X", the first question is which writer wrote it, not why the writer you have in mind would have.

## Six ways worker state has been wrong, and how each was closed

Same class each time: hive reported something it could not observe, or had the fact and did not say it plainly. The last three are the odd ones out and worth reading for the difference: there the state was **correct** and the meaning read off it was not. The fifth is the fourth arriving through the door the fourth's own fix did not cover, and the sixth is a predicate that was right about one of the three things it had to be right about.

- **A state that could not be written** (#28, closed 2026-08-08): a worker blocked on a permission prompt could not be reported, because `waiting` was latched with nothing to clear it. `noteBlockedWatched` (`src/scheduler.ts`) closed it: when a watched agent's state is `waiting` it reads that agent's pane directly and tells the wake's owner if a dialog is up, instead of waiting on a state hive cannot observe. Mechanism and edge cases in `.claude/rules/tmux-and-panes.md`.
- **A fact reported without its interpretation** (#38, closed 2026-08-02): filed as "a turn that dies mid-response leaves the worker on `working` forever and `wake_when_idle` never fires." That framing did not survive contact with the code: `max_wait_seconds` (900s default) already timed the wake out before this issue was filed, so it was never true that the wake waits forever. The real gap was narrower - `watchedTail` already embedded the agent's state and its pane tail in the wake body, but not the interpretation, so a lead reading "state now: working" next to a dead pane had to already know to distrust it. The fix folds that interpretation into the wake body directly. The paragraph below is that diagnostic; it stays true and worth knowing even with the fix shipped, since recognizing the shape on sight is still the fast path.

  **THAT FIX DID NOTHING WHEN NO WAKE FIRED, AND THAT HALF WAS CLOSED (TODO 391).** #38's remedy lives inside a wake body, and `hive doctor`/`agent_list` carry the staleness on PULL (issue #72) - so a lead that follows this project's own runbook, arms one standing watch and goes quiet, was the lead least likely to be told. Reported from two independent sites. `noteStalledCrew` (`src/scheduler.ts`) now files a stall notice through the standing watch, and `reportStalledWorkers` (`src/cli.ts`) is the sibling for a project with no watch armed at all.

  **THE SAMPLER IS THE WORKER'S TRANSCRIPT mtime, NEVER THIS LATCH, and that is the transferable half.** Measured against the live store: ten workers showed two consecutive `prompt` rows with no `stop` between them, gaps of 33 to 125 minutes; of the four checked against their own transcripts, THREE WERE ALIVE AND WRITING right up to the next prompt. A detector keyed on latch age fires four times and is wrong three times. The latch decides only whether it is worth LOOKING - the rule this file's `also_when_stuck` dead-end already states - and the transcript decides what is true. The bound is 15 minutes, argued from the Bash tool's own 600s timeout ceiling rather than fitted to the corpus; re-measure before assuming it travels to a project with a longer legitimate tool call.

  **BOTH ARMS ARE REQUIRED, and `working` is the one that does not cover the condition.** `stateForNotification` (`src/hook.ts`) returns `waiting` for every notification that is not `idle_prompt`, latched until that turn's own stop, so a turn that dies AFTER a permission prompt reads `waiting`. Measured: real workers sat `waiting` for 3.9 and 11.5 minutes, and three `notify|waiting` rows were the last row their worker ever wrote. Arm 2 covers that, and additionally requires a fresh definite "no dialog on that pane" - `null` from the probe is no fact, never "no dialog" - claiming the block key first so it can never contradict the block report. It is a REPORT, never a gate: a worker inside one very long tool call is indistinguishable from a dead turn to every sampler available.
- **A contract change nothing was watching for** (#46, closed 2026-07-31): no canary over the payload contract, so a field changing shape would not have been noticed at all. `test/payload-canary.test.mjs`, plus its committed corpus, is that canary now -- for the pure decision functions `analyzeRows`/`evaluateRun` (against a synthetic manifest) and, through `deriveManifest`/`checkPayload` in `test/payload-shape.test.mjs`, against the real committed corpus. `scripts/payload-canary.mjs`'s own live wiring (a real spawn, a real receipt) ran under no test at all until todo 388, and that gap is how the script broke silently: a receipt field the script read was renamed (todo 387), and `npm test` stayed green twice. `test/script-receipt-contract.test.mjs` now pins every receipt field the script's source reads against a real receipt; the full live run remains hand-run only, per its own header.
- **A correct idle mistaken for a finish** (#156, closed 2026-08-11): `claude --resume` replays the restored conversation, ends that turn, and fires a Stop hook, so a resumed worker goes idle - genuinely, freshly, correctly - for a turn nobody asked for. A standing watch reported that as "finished" before the worker had been given its assignment, and a lead that trusts the wake tears down a worker that never started. **Nothing about the state was wrong**, which is why every guard already on this page missed it: `agent_state_log` records the transition faithfully, `idleIsAFreshTransition` is correctly true for it, and `resumeAgent`'s own latch reset (issue #154) cannot touch an idle that has not happened yet. Closed with `agents.resumed_at`, stamped by `resumeAgent` and cleared by `src/hook.ts` on the first `prompt` event, so `resumed_at != ''` read as "resumed, and not yet spoken to" (**both halves were later widened, todo 373 - read that entry below before relying on either sentence**). **THREE surfaces answer "this worker is idle, act on it", and the fix had to reach all three** - `standingIdleRows` and `watchedStates` (`src/scheduler.ts`, the standing watch and the one-shot) and `wake_when_idle`'s own `mode="all"` already-satisfied shortcut (`src/tools/wakes.ts`), which returns before any scheduler code runs. The first version of this fix reached two of them and its own comment said "fixing only the half that was observed is how a defect ships twice"; it shipped three times, and counselors found the third. The predicate therefore lives in ONE module that names its readers - `src/stateProvenance.ts` when this shipped, `src/firstPrompt.ts` since it was moved and widened there (todo 373, below), where it is `awaitingFirstPrompt`. Reproduced before it was fixed, end to end and with no hand-written state: `test/resume-false-finish.test.mjs`.

  **SUPPRESSING A FINISH SILENTLY BREAKS THE DEATH REPORT, and that is the non-obvious half.** `standingGoneRows` excludes a closed row whose `agent_state` is `idle`, on the premise "idle means it had finished and this watch has ALREADY SAID SO". This suppression is precisely what falsifies that premise: the idle was deliberately not said. So a worker resumed at 09:00 whose restore turn ended and whose pane then died before any assignment landed was closed with the latch frozen at `idle`, and was silent in BOTH halves - no finish, no obituary, nothing in `hive status` - which is the worst outcome available, reached by two individually correct guards meeting. The gone query now reads `(agent_state != 'idle' OR resumed_at != '')`. **If you ever add a second reason to suppress a finish, this is the paragraph to re-read**: every finish-suppressing rule has to be checked against every rule that reads "already reported" off the same column.

  **A PARK IS NOT A DEATH EITHER.** `agent_park` (issue #156) closes the row deliberately, and a worker parked mid-work reads `working`, so without an exclusion every park filed an obituary telling the lead that worker died and to go and check its branch for what was lost - one per worker, at exactly the moment a lead parks a crew at the end of the day. `standingGoneRows` excludes `parked_at != ''` for the same reason it excludes a close from `idle`.

  **BOTH OF THOSE EXCLUSIONS READ COLUMNS ANOTHER WRITER CAN CLEAR, WHICH IS A THIRD DOOR INTO THE SAME FALSE OBITUARY (todo 374).** `resumeAgent`'s flip un-closes a row in one statement that also clears the park stamp AND resets `agent_state` to `unknown`, and its pre-pane failure path used to restore `status`/`closed_at` and nothing else. So a resume that died before its pane came up left a closed row with BOTH exclusions gone - `parked_at` cleared, `agent_state` no longer `idle` - plus a freshly stamped `closed_at`, which is the episode key every gone cursor and every `seedGoneCursor` row is written against. Nothing had ever recorded a cursor for the parked lane either, because the park exclusion is a FILTER rather than a claim. Every clause passed and the lead was told a lane it had parked the night before had died. Note the second half stands alone: an ORDINARY closed row that closed from `idle`, never parked at all, loses ITS exclusion by the same route. Closed by making the revert the flip's exact inverse rather than a list of the columns somebody noticed - see `RESUME_FLIP_COLUMNS` (`src/spawn.ts`), pinned by `test/resume-revert-restores-row.test.mjs`. **The transferable half: an exclusion is only as durable as the column it reads, so when you add one, ask who else writes that column and what their failure paths put back.**

  **The transferable half is the question to ask, not the column.** "Which writer wrote this value" (the section below) was the right question for #24 and answers nothing here, because the right writer wrote the right value. The question that finds this class is **"what did the worker DO to earn this state, and did anyone ask for it"** - and that is answerable from the same log, since a finish nobody asked for is an idle with no `prompt` before it.

  **It only ever suppresses, and that is what keeps it out of `also_when_stuck`'s grave** (`.agents/sessions/dead-ends/2026-07-29-also-when-stuck-on-latched-waiting.md`). That design FIRED on a latched state whose end emitted nothing. This one fires nothing, and the end of its condition emits a `prompt` hook hive already wires and already writes on. Read the dead-end's rule as it is written: a latch may decide whether to LOOK, never what is true. Deciding to STAY QUIET is the safe direction of the same rule.

  **The accepted residual, so nobody rediscovers it as a defect.** A delivery into a BUSY pane is absorbed into the running turn as an attachment and fires no `UserPromptSubmit` at all (`.claude/rules/tmux-and-panes.md`), so an assignment sent while the restore turn is still replaying leaves `resumed_at` set and that turn's real finish is suppressed too. Bounded, not permanent: the next delivery landing on an idle pane is a real user turn and clears the column. **Say what hive does and does not know here, because "no evidence of any kind exists" is too strong and was the recorded wording until todo 377.** hive's OWN delivery sites - `agent_send`'s text path and `deliver()` - are first-person evidence that something WAS given to this worker. What hive cannot tell is whether the in-flight turn's end includes that work: between a restore turn and an attachment-driven turn there is no `prompt` and no `working` latch to separate them. So the limit is on attributing the FINISH, not on knowing a delivery happened.

  **AND THE SILENCE IS NOW REPORTED, WHICH IS THE ONLY THING AVAILABLE (todo 377).** Every way the clearer fails to run produces one signature - a running `kind='agent'` row with `resumed_at` set, going on being suppressed - so `hive doctor` names such a worker past a **GUESSED** 30-minute bound (`reportUnbriefedWorkers`, `src/cli.ts`). Information, never a gate, on `reportPtyHeadroom`'s stance. It does not fix the absorbed delivery and must not be read as if it did; it converts "a lead waits forever" into "hive says which worker it has gone quiet about". Its headline claims only what hive OBSERVED - the latch has been set this long - never *why*, because the commonest route here is the absorbed delivery, where the worker was briefed and is working normally and only its FINISHES are lost.

  **THAT REPORT IS THE FIRST READER OF THIS COLUMN THAT DOES NOT SUPPRESS, AND IT FALSIFIED A RECORDED JUSTIFICATION (counselors, two seats).** `launchAgent`'s INSERT stamps `resumed_at` for every kind, and its own comment defended that on the grounds that an uninstrumented row "never suppresses anything either, because its `agent_state` stays `unknown` and every reader here is gated on `idle`". True of every reader that existed when it was written; a reader that REPORTS was added (todo 377). `agent_spawn` sets `kind='agent'` for every command, so a bash or codex worker is a `kind='agent'` row whose latch can never clear - a non-claude pane fires no `UserPromptSubmit` - and it would have been named on every run for the life of the row, with a remedy that cannot work. Gated on `reportsAgentStateLog` (`src/stateProvenance.ts`), this project's own allowlist for "this row has a state channel". **The rule for the next reader of this column: if it SUPPRESSES, the `idle` gate covers you; if it REPORTS, ask whether the row has a state channel at all before you say anything about it.**

- **The same false finish, arriving through spawn** (Todo 373, closed 2026-08-12): this is #156's defect through the ordinary dispatch path. `agent_spawn` types a `[hive]` line into a brand-new worker's pane and SUBMITS it, so the worker answers it, that turn ends, Stop fires, and the row latches idle before anybody has given that worker a lane. The resume end was closed, leaving this one open (#156), so the false finish fired on EVERY spawn made while a standing watch was up - which the runbook tells every lead to have. Watched four times in one evening. Reproduced end to end first: `test/spawn-false-finish.test.mjs`.

  **THE RESUME FIX DOES NOT TRANSFER, AND THE REASON IS THE WHOLE LANE.** The fix clears its latch on the worker's first `prompt` event (#156), because a resume types NOTHING into the pane - so the first prompt really is the first moment somebody gave that worker something. A spawn speaks first, and **its announcement is a real `UserPromptSubmit`**. Measured on todo 373's own worker (`agent_state_log` for `agent:208`): `prompt|working` at 03:49:25 (hive's announcement), `stop|idle` at 03:49:30 (the false finish), `prompt|working` at 03:49:40 (the lead's actual assignment). A latch cleared on the first prompt is cleared five seconds before the idle it exists to suppress, and the fix ships green and does nothing. **If you extend this suppression to a third start path, ask what hive itself types into that pane before you reuse the clearing rule.**

  Closed by giving the clearing rule the one exception the announcement's own text already implies ("wait for your assignment"): `src/hook.ts` clears on the first `prompt` that is **not** hive's spawn announcement, discriminated by `isSpawnAnnouncement` on a constant `paneAnnouncement` itself is built from, so the two cannot drift. Matching hive's own text in a prompt payload is not new here - `checkConfirmations` recognises a delivered wake by its `[hive wake #N]` prefix the same way. A stamp written AFTER the announcement was typed was rejected: the hook fires in the worker's process while `sendText` is returning, so it is a race, and losing it reports the finish.

  **ONE COLUMN, NOT TWO, AND THE DECIDING READER IS A `WHERE` CLAUSE.** `agents.resumed_at` now means "started (spawned or resumed), and not yet given anything" - `launchAgent` stamps it in its INSERT, `resumeAgent` in its flip - and its NAME is half the truth, accepted rather than renamed. A second column would have to be OR'd in by every reader, and one of those readers is `standingGoneRows`'s SQL, which cannot call a JS predicate; widening one column means the finish-suppressing rule and the "already reported" rule above stay paired with **no edit to that query at all**. A `RENAME COLUMN` migration is append-only-legal and was rejected too: an MCP server started before it keeps running old `dist/` for the life of its session and would hit `no such column` inside the scheduler tick.

  **The predicate moved out of `src/stateProvenance.ts` to `src/firstPrompt.ts`, a leaf module with no imports**, and the move is forced rather than tidying: `src/dashboard.ts` is two of the readers (todo 366) and is deliberately free of any tmux dependency, while `stateProvenance.ts` imports `src/tmux.ts`.   **SUPPRESSING A FINISH IS NOT THE WHOLE JOB: SOMETHING ELSE THEN CLAIMS THE WORKER IS NOT THERE.** All three counselors seats overturned this lane's own recorded acceptance of one gap, independently, and the shape generalises past the instance. A standing watch's "Still going" roster filtered `agent_state != 'idle'`, so a suppressed worker dropped out of it - and an empty roster makes the notice say *"Nothing else in this project is running right now"*, an affirmative claim about the crew. The lane had recorded that as honest silence. It is not silence, and the minimum shape is the ordinary wave: A is assigned and finishes, B was spawned alongside it and not yet briefed, and A's notice denies B exists. **The pairing rule above, restated for this direction: every rule that SUPPRESSES has to be checked against every rule that says "and therefore nothing else is there".** The same audit found `hive status` and the SessionStart digest (`src/kickoff.ts`) printing "idle for 3m" for the same worker through `describeForHuman`; the digest is *injected into a fresh lead's context* next to the instruction to triage it, so a model consumes that sentence and proposes lanes off it. Both corrected, both pinned, and the exclusion that had kept `agent_status`/`agent_list` out ("they report the row rather than deciding on it") was WITHDRAWN in writing rather than routed around: the dashboard badges only report too, and this lane fixed them.

- **Todo 387** (closed 2026-08-13): **todo 373's own fix, above, is what makes this entry historical rather than current for the spawn side.** `agent_spawn` typing and submitting a `[hive]` line was the turn every paragraph above exists to manage - and the deeper cost was found (todo 384): if a lead's real assignment landed while that self-inflicted turn was still running, it could be absorbed into it as an attachment with no `UserPromptSubmit` to clear the latch, and since the ordinary dispatch shape is brief once and wait, the suppression was **operationally permanent** for that worker - not the "bounded, not permanent" residual recorded (todo 373). An adversarial pass (agent:216) then found the announcement's own drain path was not the only one (`.claude/rules/tmux-and-panes.md`'s busy-pane section now says so), and proposed removing the turn rather than making its consequence recoverable: **nothing is typed into a spawned worker's pane anymore.** `SPAWN_ANNOUNCEMENT_PREFIX`, `isSpawnAnnouncement`, and `src/hook.ts`'s exception for the announcement's own prompt are all gone. `launchAgent`'s INSERT no longer stamps `resumed_at`; only `resumeAgent`'s flip does, so the column is back to meaning exactly what its name says - "resumed, and not yet given anything" - and every paragraph above about a SPAWNED worker's false finish describes a defect that structurally cannot recur, because there is no longer a turn for a busy-pane assignment to land inside. **What did not change**: resume's own restore turn is real Claude Code behaviour, not something hive types, so it is untouched, and everything above about the resume half stays current. `test/spawn-false-finish.test.mjs` now asserts the replacement invariant - a spawned worker has no turn at all until briefed - rather than the suppress-then-clear mechanism this entry describes.

- **A predicate that knew one of three types** (todo 468, closed 2026-08-20): `waitingOnSubagents` withheld a worker's idle for `type === "subagent"` and nothing else, and Claude Code's Stop payload carries three types. MEASURED over this project's own `agent_state_log`, all time: 138 of 1,126 stop events (12.3%) latched a row idle while a NON-TERMINAL `shell` sat in the payload, up to four distinct agents in a day, and the task descriptions are the ordinary dispatch path ("Run the granted full suite in the background", "Wait for counselors to finish"). OBSERVED LIVE during wave 8: wakes #862, #864, #865, #871, #873 and #874 were all one worker latching idle against a background shell it had started, delivered to the lead's pane as "1 finished" while the pane read "1 shell still running". Six lead turns in one wave, and the two things being waited on were a granted full-suite run and an 11-minute CI leg, so the false finish fires hardest where the wait is longest and a lead is most likely to act on the notice rather than the pane.

  **THE FIX IS IN THE READER, NOT THE LATCH, AND THAT IS THE WHOLE DESIGN.** Widening the predicate to `shell` is the obvious move and it is silent in the worse direction: a shell need never terminate, so a worker that leaves a watcher, a tail or a wedged command running would never read idle, and the standing watch that exists to return control would never fire. `agent_state` is also the one column six surfaces read, so widening it widens all six. The latch keeps ONE type; the standing notice reads the live tasks and names them, in both renders (`liveTasksAtIdle` -> `backgroundTaskSentence` and the short lead render, `src/scheduler.ts`).

  **`monitor` WAS DECIDED ON DATA, NOT JUDGMENT.** All 19 monitor rows in the live store come from one actor and every entry is an artifact live-update watcher auto-armed on publish - three running at once, none of which ever terminates. Had `monitor` withheld idle, that session could never have read idle at all. It is named in the notice and never latches. No monitor has ever appeared on a WORKER row, so naming it costs nothing observed.

  **THE FACT COMES FROM THE RAW STOP PAYLOAD, NOT A NEW COLUMN.** `liveTasksAtIdle` reads `agent_state_log` filtered to `event='stop'` and floored at the episode being reported, so it names its writer rather than assuming one - the `notify` branch's `unchanged` rows are what hid issue #38's stall. A denormalized `agents` column was rejected twice over: it is the projection this table is stored raw to avoid, and it would be written only on stop, so it would go stale the moment the worker resumed.

  **THE GUARD IS A TABLE, NOT A PROMISE.** `BACKGROUND_TASK_DISPOSITION` (`src/backgroundTasks.ts`) is pinned as an exact set by `test/false-idle.test.mjs`, so a fourth type cannot be added to hive without saying whether it withholds idle. And the READER is type-agnostic by construction: it names any non-terminal task whatever its type, so a fourth type Claude Code introduces degrades to being named rather than vanishing. That is the choke point `common-issues/a-fix-applied-to-only-some-call-sites.md` asks for instead of an enumeration.

**The reader list lives in that file and is authoritative there, not here** - a count restated in prose is a count that goes stale the next time somebody adds a reader, which is the same failure this entry is about. What is worth carrying: the readers span `src/scheduler.ts` (including one that is a SQL `WHERE` clause), `src/tools/wakes.ts` and `src/dashboard.ts`, plus the writer that clears the column, and **two of them are in one file** - which is why todo 366's own "the dashboard is a fourth reader" undercounted. Count the badge sites, not the files.

Spotting the pattern is still cheap: a worker whose last `agent_state_log` row is a `prompt|working` with no `stop` after it, whose pane shows an error and an empty input box, and whose branch has not moved. A `working` older than the lane's rhythm is suspicious; `working` is not self-evidently healthy. Recovery is to send it a message, and to tell it what state you found, because after an API error it does not reliably remember what it was doing.

## A debounce is not an inference

Both appeared in one lane and only one was legitimate. The notify branch asserted a fact it could not see: the input box has been quiet for sixty seconds, therefore the worker is finished. It was deleted for that. A dwell that waits for a state to stop changing and reports what it observed asserts nothing, is self-verifying, and can only make a wake late rather than early. If you write one, say in the code which kind it is, because the next reader cannot tell from the shape.

## Never set a /goal on a worker

A goal fires the Stop hook after every turn while immediately starting another, so hive records idle for a worker that never stopped. Nine consecutive false idles were measured on agent:53 in 50 seconds with no `prompt|working` between them. `agent_state_log` does not save you here: the rows are not corrupt, they are each briefly true and instantly stale. The lead has an agents row now (issue #27) and writes hook rows like any other actor, but stays safe because its hook writes no STATE row: `src/hook.ts`'s `agents.agent_state` UPDATE is scoped to `WHERE ... AND kind = 'agent'`, an allowlist rather than a lead-specific skip (so a future third kind defaults to the same silence, not to getting state written by accident), so this exact churn lands in `agent_state_log` only, where it is harmless and best-effort forensics rather than a false idle something else acts on.

## The codex subagent latch is bounded and keyed per agent_id, not a single newest-row toggle (todo 525)

R4 (research pad 229, todo 508) proved codex's own hook payloads are Claude Code's field names,
string for string, with one exception: codex's Stop payload never carries `background_tasks` at all
(structurally absent across every trial, not omitted by chance), so `waitingOnSubagents` could never
withhold idle for a codex worker's live subagent the way it does for claude's. Codex reports the same
fact through two dedicated events instead - `SubagentStart`/`SubagentStop`, both fired cleanly in
every trial R4 ran - and `src/hook.ts`'s `stateFor` records them log-only (like `idle_prompt`'s
`null`), leaving the actual decision to a `Stop` handler that reads the log back.

The first version of that reader (`hasOpenSubagent`) took the single newest of the two event types
per actor and asked whether it was a `subagent_start` - a stack of one, not a set. Code review on
todo 525 found three ways that model is wrong, all the same root cause seen from a different side:

- **A crashed or killed subagent never fires `SubagentStop`.** An unmatched `subagent_start` was
  therefore the permanent newest row, and the latch withheld idle forever - worse, silently: codex
  only earns `stateSource`, not `transcriptDir` (transcript tailing is a separate, unproven
  mechanism), so this stuck worker was also excluded from both stall-report paths
  (`reportStalledWorkers` in `src/cli.ts`, `noteStalledCrew` in `src/scheduler.ts`), which corroborate
  a latch against transcript mtime and cannot safely do that for a harness with no proven transcript
  path. Silent and undetectable together is the worst combination this project has.
- **Two concurrent subagents collapse to one toggle.** `start(A) -> start(B) -> stop(A) -> stop` reads
  the newest row as `subagent_stop`, so the latch releases while B is still running. Not hypothetical:
  claude's own captured corpus (`test/fixtures/hook-payloads/stop-subagents-running.json`) shows four
  concurrent subagents in one real `Stop` payload.
- **Retention can evict the only evidence.** `pruneStateLog` (`src/scheduler.ts`) caps
  `agent_state_log` at `LOG_MAX_ROWS` (20,000) globally, not per actor. If a `subagent_start` row is
  pruned before its `subagent_stop` arrives, the query finds neither row and reads `false` - the
  opposite failure direction from the two above (there the latch sticks too long; here it releases
  too early).

**The fix is a model change, not three patches.** `hasOpenSubagent` now reduces every
`subagent_start`/`subagent_stop` row for the actor **within `SUBAGENT_LATCH_MAX_AGE_SECONDS`**
(`src/backgroundTasks.ts`, 15 minutes - DERIVED from `STALL_BOUND_SECONDS` in that same file,
not a second literal, since past that age hive already treats an ordinary latched worker as worth a
stall report. Keep it a reference: two equal literals drifted apart silently once already) into a **set of open `agent_id`s**, in order: a `subagent_start` adds its id, a
`subagent_stop` removes it, and the latch withholds idle exactly when that set is non-empty.

Checked against the new model, none of the three can recur as a DISTINCT failure:

- **Crash**: an unmatched `subagent_start` ages out of the window on its own. The latch cannot stick
  forever - past 15 minutes it releases regardless of whether a `SubagentStop` ever arrives, so stall
  reporting never has to catch it (it structurally cannot get stuck in the first place).
- **Concurrency**: each subagent is tracked by its own `agent_id`, so one subagent's stop can never
  close a different one's still-open start.
- **Retention eviction**: this is the one residual worth naming precisely rather than claiming closed.
  Deleting a row produces the identical output the age bound already produces once a row ages past
  it - "no evidence found" reads as "not open" either way, because the log-derived model has no way to
  distinguish "this subagent finished" from "hive can no longer see whether it finished." What changed
  is not the mechanical output for this input (it was already `false` in the old model too, checked
  directly), but the ARGUMENT for why that is acceptable: the old model's unstated intent was to hold
  the latch until a real `SubagentStop` arrived, so an early eviction silently violated a promise
  nothing else in the design admitted making. The new model's stated intent is "at most 15 minutes,"
  so an eviction that releases the latch early merely delivers a possibly-sooner instance of an
  already-accepted bound, not a new kind of wrong answer. `LOG_MAX_ROWS` at 20,000 rows makes an
  in-window eviction (more than 20,000 other log rows landing globally within the same 15 minutes)
  implausible at any usage this project has observed, but it is not proven impossible, and retention's
  own logic (the janitor, not the latch) was out of scope to change here.

`test/hook-subagent-latch.test.mjs` proves the crash and concurrency cases red against the old
newest-row-only code before the fix, then green after; the retention test passes unchanged in both
versions, by construction, since (as above) neither model's output for that input actually differs -
it pins the residual rather than a transition.

## Wake-up bodies are delivered verbatim into a terminal

Whatever you pass as a wake body is typed into the target pane exactly as written, and it becomes a fresh user turn only if that pane happens to be idle. So write it as plain English that stands on its own: the ids it refers to, the context needed to act, and the next action.

The failure this prevents is a body that only parses as a reply. "Yes, go ahead with option 2" is unreadable when it lands mid-turn hours later next to work that has moved on, and there is no thread for the reader to scroll back to. Assume the reader has none of the conversation that produced the wake, because usually it does not.

**A delivery into a BUSY pane may not be confirmed, so `unconfirmed` does not mean undelivered.** Do not read that as a failure and do not build anything that waits for a late confirmation. Mechanism, including the drain-path race and its dated measurements, is in `.claude/skills/hive-internals/references/tmux-and-panes.md`. Separately, two paths exist where a wake genuinely never fires, in `.claude/rules/tmux-and-panes.md`: a pane sitting on a dialog, and a pane holding unsubmitted human text. Both hold past `max_wait_at`, and the second was added deliberately - delivery pastes and presses Enter, so a box with text already in it would submit the wake merged with whatever the human was typing.

## The two generated exceptions to verbatim delivery, and what each was measured at (todo 473)

Both ride on a standing watch's finish notice and neither has an author, so neither is covered by the verbatim rule. Both were measured on the lead's own pane on 2026-08-20/21, and both were invisible until someone read the DELIVERED string rather than the stored body - the 455/456 lane and todo 465's lane each measured the body and missed them.

**The crew render.** `shortRenderForLeadDelivery` builds one line per WORKER from the notice's own `wake_idle_notices` claim rows. It used to build one entry per CLAIM, which put three separate wrong beliefs in a lead's pane in one evening:

- Wake #892, held about 12 minutes by the conversation hold, delivered `4 finished: t475-quiet-lead-pane, t475-quiet-lead-pane, t475-quiet-lead-pane, t475-quiet-lead-pane (bg). 1 went idle with background tasks running and may not be done (bg). 0 still going.` Verified against the store rather than eyeballed: `SELECT COUNT(*), COUNT(DISTINCT agent_id) FROM wake_idle_notices WHERE notice_timer_id = 892` returned 4 claims and 1 agent. A count beside a list of names reads as a headcount, so a lead acting on it believes its crew is four times its real size.
- Wake #891 delivered `1 finished: t475-quiet-lead-pane. 1 still going.` with exactly one worker in the project. The finish is claim-time and the roster is delivery-time: the notice was held 4 minutes and the worker had taken new work by the time it rendered, so the same worker was counted once in each half of one sentence.
- Both predate `d11c028`; the `(bg)` markers that commit added were correct in every case.

Chris settled the fork on 2026-08-21: the notice reports CREW STATE, not episodes, because a lead does not act on the fact that a worker finished four turns - it acts on whether that worker is done and whether to go and look. The reversal cost is recorded with the decision: an episode-shaped render is a different render, not a tweak to this one.

**The staleness trailer.** `noticeStalenessNote` had no threshold. On wake #885 it appended 138 characters to a 72-character notice held ONE second, so the provenance was 66% of the wake and it was the half that said nothing: at a short hold `heldSince` and `timer.created_at` are the same instant, so the two sentences that carefully distinguish them printed the same timestamp and the same age twice. Wakes #880 through #885 all delivered with holds of 1-2 seconds. The bound is `CONVERSATION_HOLD_TTL_SECONDS` rather than a number of its own, because the conversation hold is what produces these delays; it gates both printing at all and collapsing the two clauses into one. It is not truncated: the long-hold case is what it was built for (the 2026-08-13 stranded-paste incident), and that case still gets it in full.

## A standing watch reports its owner's crew, not the whole project (todo 455)

`OWNED_BY_WATCH` (`src/scheduler.ts`) scopes `standingIdleRows`, `standingGoneRows`, and the still-going roster to agents whose `parent_actor_id` is the watch's own owner, or NULL. Measured against the live store on 2026-08-20: 282 `kind='agent'` rows, 0 NULL, 204 parented by a `lead:`, 24 by an `agent:`, 54 by a `user:`. Every noise notice that night traced to the 24 `agent:`-parented rows - a worker's own throwaway probe, spawned and abandoned without telling anyone; every notice the lead actually needed traced to a `lead:`-parented row.

A `disposable: true` flag on `agent_spawn` was considered and rejected: it would need an append-only migration for something the store already knows, and only works if every lane remembers to set it.

Two tradeoffs accepted rather than closed, both decided in todo 455 comment 1369:

- A worker that spawns a probe and abandons it now dies unreported to the lead. That is the parent worker's own problem to notice, not the watch owner's - "tell me about the workers I dispatched" is what a standing watch has always meant for its owner.
- A lead's own throwaway (`parent_actor_id` = the lead) still notifies. Rarer than the worker case, and arguably correct, but not covered by this filter.

`stallCandidateRows`/`stallNoticeBody` (the blocked/stalled path) deliberately do NOT get this filter: a blocked worker's wake is the only surface that will ever report the news, so a blocked grandchild stays loud on purpose (docs/daily-driver.md, docs/troubleshooting.md both promise this in print).

This narrows the project-scope definition in `.agents/sessions/decisions/2026-08-08-watch-membership-is-a-parameter.md`, which is amended in place with a dated note; the cross-lead scoping question that decision left open is still open.

## The conversation hold: signal, TTL, and its ceiling (todo 455)

`conversationHoldsWake` (`src/scheduler.ts`) holds a lead-bound wake (`HELD_REASON_CONVERSATION`) when the lead's most recent `agent_state_log` prompt row is human-authored (its payload does not carry the `[hive wake #` prefix hive's own typed deliveries always add) and newer than `CONVERSATION_HOLD_TTL` (5 minutes). No new collection: the lead's own hook already appends every human turn to `agent_state_log` with the raw payload, unused until this fix. `inputBoxHoldsWake`/`holdsHumanInput` is deliberately NOT reused for this signal - that predicate answers "does the input box have non-faint characters right now", and an empty box is "done talking", "thinking", "reading a diff", and "went to lunch" all at once; it cannot tell hive's own stranded paste from a human mid-sentence, which held every later wake indefinitely on 2026-08-13.

Measured against this project's own store: a 5-minute TTL defers 28% of lead-pane deliveries, 10 minutes defers 45%.

**The ceiling is measured against `due_at`, not `first_held_at`, and this was a real bug in the first cut.** A notice held past `NOTICE_MAX_AGE` (currently one hour) is cancelled by `noticeDisposition` rather than delivered, so the conversation hold carries its own `CONVERSATION_HOLD_MAX` (15 minutes, a 4x margin under the hour) past which it stops applying regardless of how recently the human spoke. The first cut measured that ceiling against `timer.first_held_at` - but `hive lead`'s pane re-point (`src/cli.ts`, on every ordinary reattach, not just crash recovery) clears `held_at`/`held_reason` unconditionally while leaving `first_held_at` untouched, and `holdTimer`'s own COALESCE resets `first_held_at` whenever it finds `held_at IS NULL`. So a routine `hive lead` reattach mid-conversation laundered the ceiling's clock, and repeated reattaches could extend the hold indefinitely - past `CONVERSATION_HOLD_MAX`, and eventually past `NOTICE_MAX_AGE` itself, silently destroying the very notice the ceiling exists to protect. `due_at` is stamped once at creation (`insertNotice`) and nothing resets it, so it cannot be laundered by a pane re-point. DO NOT READ THAT AS ONE CLOCK SHARED WITH `NOTICE_MAX_AGE`: that bound is measured against `created_at`, which `updateNoticeInPlace` RESETS every time a new finish merges into a pending notice, so the two diverge by the merge span. The divergence runs the safe way - the 15-minute ceiling expires earlier than the hour it protects against, and more so the busier the watch - but a reader reasoning from one clock will get the margin wrong.

Under a `/goal` there is no human turn between Stop-hook firings (`.agents/sessions/decisions/2026-08-09-a-goal-defers-wakes-so-an-unattended-loop-polls.md`), so nothing refreshes the window from the human side and the hold ordinarily stays out of the way - correct and intended, not a gap. DO NOT READ THAT AS UNREACHABLE UNDER A GOAL. The query matches any `prompt` row for the lead actor whose payload lacks the `[hive wake #` marker, and `agent_send`'s text path is permitted against a lead from any caller (only raw `keys` is gated, `src/tools/agents.ts`), so a worker reporting in refreshes the window exactly as a human would. An unattended run whose crew messages its lead holds each wake until the crew goes quiet or the `due_at` ceiling passes. Whether an agent-authored prompt SHOULD count as a conversation is open - todo 467.

`NOTICE_MAX_AGE`'s destruction mechanism itself (any long hold, of any kind, can age a notice past it) is pre-existing and was not fixed by this ceiling - only made unreachable through this one hold. It stopped being SILENT in todo 465: `ageOutNotice` cancels the notice and files a short replacement to the same target in one transaction, naming the workers it covered and pointing at `wake_get` on the cancelled row, which still holds the full text.

**TODO 465 DELIBERATELY DID NOT MOVE THE BOUND ONTO `due_at`,** even though the paragraph above establishes that `due_at` is the un-launderable clock and it would make the two consistent. `created_at` is reset by coalescing, so today's bound means "the freshest fact in this notice is under an hour old" - a defensible fact, and the body already reports the span it covers. Moving to `due_at` would start destroying a BUSY notice at one hour that currently survives a long hold and delivers in full, which spreads the quiet watch's failure to the busy one under cover of a consistency fix. The divergence is real, it is documented above, and it is not the defect. What was the defect is that the cancel wrote nothing anywhere: the finish's episode row in `wake_idle_notices` stays CLAIMED, so the watch can never report it again.

**The replacement carries no parent, and that is load-bearing rather than incidental.** `noticeDisposition` short-circuits to `deliver` for a parentless notice, so the thing that reports an age-out cannot age out in its turn. Releasing the episode claim so the notice re-queues was the other candidate and it loops: it would be filed again, held again, and aged out again. `noticeStillDeliverable` became `noticeDisposition` in the same change because its two false cases earn opposite endings - a cancelled WATCH is a lead asking for no more notices and stays silent, an aged-out notice is a finish about to vanish and speaks.
