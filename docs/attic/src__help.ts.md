# Attic: src/help.ts

Comments removed from `src/help.ts` by todo 436, verbatim. Line numbers are
positions in the pre-strip file at fed8064.

## line 1

```
// A function so the topic list is derived from HELP_TOPICS (defined below)
// and cannot drift.
```
